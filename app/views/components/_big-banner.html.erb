<% if banner != "" %>
	
	<%= link_to banner.url, class: "banner--big" do %>
		<img 
			src="<%= banner.large_image %>" 
			srcset="<%= banner.large_image %> 1024w, <%= banner.small_image %> 1023w"
			alt="<%= banner.name %>">
	<% end %>
<% end %>

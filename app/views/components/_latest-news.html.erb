<div class="latest-news">
	<div class="special-articles__type">
		<p class="type">Minuto a minuto</p>
		<p class="link">&nbsp; </p>

	</div>
	<div id="latest_news" >
		
	</div>
	<% articles.each do |article| %>
		<% if article.articable_id != 1019   %>
					
			<%= link_to article, class: "latest-news__article" do %>
				<div class="clock">
					<div class="hours"></div>
					<div class="minutes"></div>
				</div>
				<% if article.published_at %>
					<p class="article__time"><%= article.published_at.strftime("%H:%M") %></p>	
				<% else %>
				<% end %>
				<p class="article__title"><%= truncate(article.name, length: 55) %><br></p>
			<% end %>
		<% end %>
		
	<% end %>
</div>


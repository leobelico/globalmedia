<% if type == "Investigación Especial" %>
	<div class="special-articles investigation">
<% else %>
	<div class="special-articles">
<% end %>

	<% if type == "#DENUNCIAGLOBAL" %>

		<%= link_to section_path(Section.find_by(name: "Denuncia Global")), class: "special-articles__type" do %>
			<p class="type">#DENUNCIAGLOBAL</p>
			<div class="link">Ir a sección</div>		
		<% end %>
		<h4 class="special-articles__title" data-link="<%= section_path(Section.find_by(name: "Denuncia Global")) %>"><%= articles.first.name %></h4>
	<% else %>
		
		<%= link_to relationship_path(Relationship.where(relationship_type: "Investigation").last), class: "special-articles__type" do %>
			<p class="type">Investigación Especial</p>
			<div class="link">Ir a sección</div>

		<% end %>
		<h4 class="special-articles__title" data-link="<%= relationship_path(Relationship.where(relationship_type: "Investigation").last) %>"><%= title %></h4>
		
	<% end %>


	<% if articles.first.video_url == "" %>
		<% if title != "Denuncia Global" %>
			<% Relationship.where("relationship_type = 'Investigation'").last.image %>
			<%= image_tag Relationship.where("relationship_type = 'Investigation'").last.image, class: "special-articles__image", data: { link: section_path(Section.find_by(name: "Denuncia Global")) } %>
		<% else %>
			<%= image_tag Article.where("articable_id = 12").last.image, class: "special-articles__image", data: { link: relationship_path(Relationship.where(relationship_type: "Investigation").last) } %>
		<% end %>
	<% else %>
		<iframe src="<%= articles.first.video_url %>" frameborder="0" allowfullscreen width="100%" height="280px"></iframe>	
	<% end %>
	<%= link_to articles[0].name, articles[0], class: "special-articles__article" %>
	<% articles[1..-1].each do |article| %>
		<%= link_to article.name, article, class: "special-articles__article" %>
	<% end %>
</div>

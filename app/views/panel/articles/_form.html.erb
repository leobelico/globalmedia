<form id="fields-for-s3">
  <% @s3_direct_post.fields.map do |name, value| %>
    <input type="hidden" name="<%= name %>" value="<%= value %>" />
  <% end %>
</form>

<%= form_for [:panel, @article], multipart: true, html: { class: "directUpload", remote: true, data: { 'form-data' => (@s3_direct_post.fields), 'url' => @s3_direct_post.url, 'host' => URI.parse(@s3_direct_post.url).host } } do |f| %>
  <% if @article.errors.any? %>  
    <div class="card">
      <h5><%= pluralize(@article.errors.count, "error") %></h5>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card admin-form form--article">
    <% if current_user.video_complaints_permission? %>
      <div class="field field--string">
        <label>Sección</label>
        <%= f.select :articable_id, options_for_select(Section.all.collect{ |u| [u.name, u.id] }, @article.articable_id) %>
        <%= f.hidden_field :articable_type, value: "Section" %>
      </div>
    <% else %>
      <div class="field field--string">
        <label>Sección</label>
        <%= f.select :articable_id, options_for_select(Section.where("name != 'Denuncia Global'").collect{ |u| [u.name, u.id] }, @article.articable_id) %>
        <%= f.hidden_field :articable_type, value: "Section" %>
      </div>
    <% end %>

    <% if Author.all.count > 0 %>
      <div class="field field--string">
        <label>Autor de la Nota</label>
        <%= f.select :author_id, options_for_select(Author.all.collect{ |u| [u.name, u.id] }, @article.author_id), prompt: "Selecciona un Autor" %> 
      </div>
    <% end %>  

    <div class="field field--datetime">
      <label>Fecha programada:</label>
      <div class="field">
        <%= f.datetime_select :scheduled_time, datetime_separator: "<span>-</span>", time_separator: "<span>:</span>" %>
      </div>
    </div>

    <% if current_user.is_news_chief? %>
      <div class="field field--string">
        <label for="">Estatus de la nota</label>
        <%= f.select :draft, options_for_select([['Borrador', 0], ['Aprobado', 1], ['No Aprobado', -1], ['Publicar', 2]], @article.draft) %>
      </div>
    <% end %>

    <div class="field field--string">
      <label>Título de la Nota</label>
      <%= f.text_field :name, max_chars: 120, required: true, data: { max_chars: 120 } %>
    </div>

    <div class="field">
      <label>Extracto</label>
      <%= f.text_area :short_description, required: true, data: { max_chars: 150 } %>
    </div>

    <% if @article.image %>
      <div class="field field--string">
        <% if @article.image != "" %>
          <%= image_tag @article.image, class: "form__image-preview shown" %>
        <% end %>
        <label>Imagen principal actual</label>
        <input type="text" disabled value="<%= @article.image %>">
      </div>  
    <% end %>

	<div id="note-editor"></div>
	<%= f.hidden_field :note, id: 'article_note' %>

    <div class="field">
      <label>Exclusiva</label>
      <%= f.select :exclusive, options_for_select([['Sí, esta nota es exclusiva', true], ['No, esta nota no es exclusiva', false]], @article.exclusive) %>
    </div>

    <div class="field">
      <label>Hashtags: </label>
      <%= f.autocomplete_field(:hashtags_names, autocomplete_hashtag_name_panel_hashtags_path, id_element: 'hashtags', class: "search-bar", placeholder: "Por ejemplo: TECNOLOGÍA, PROGRAMACIÓN, NOTICIAS, MÉXICO", type: "search", value: @hashtags, "data-delimiter": ', ', id: 'hashtags') %>
    </div>

    <div class="field field--string">
      <label>Imagen principal</label>
      <%= f.file_field :image, required: true %>
    </div>


  <div class="field field--string">
    <label>Metatags:</label>
    <%= f.text_field :meta_tags, id: "meta_tags", maxlength: "100" %>
    <span id="meta-tags-counter" class="char-counter" style="display:block;font-size:0.8em;color:#666;">0/3 palabras</span>
  </div>

    <div class="field field--checkbox">
      <label>Último momento:</label>
      <%= f.check_box :breaking_news, id: "breaking_news" %>
    </div>
  </div>
<% end %>
<!-- Scripts necesarios de redes -->
<script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
<script async src="//www.instagram.com/embed.js"></script>
<script async src="https://www.tiktok.com/embed.js"></script>

<!-- Quill -->
<link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
<script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<script>
// Variable global para controlar el estado
window.articleFormInitialized = false;

function initializeArticleForm() {
  // Si ya está inicializado, salir
  if (window.articleFormInitialized) return;
  
  // Verificar que todos los elementos necesarios existen
  const noteEditor = document.getElementById('note-editor');
  const articleName = document.getElementById('article_name');
  const shortDescription = document.getElementById('article_short_description');
  
  if (!noteEditor || !articleName || !shortDescription) {
    console.warn('Elementos del formulario no encontrados, reintentando...');
    setTimeout(initializeArticleForm, 100);
    return;
  }

  console.log('Inicializando formulario de artículo...');
  
  // Inicializar Quill solo si no existe
  if (typeof window.editor === 'undefined' || window.editor === null) {
    initializeQuillEditor();
  }
  
  // Inicializar contadores
  initializeCounters();
  
  // Inicializar preview de imágenes
  initializeImagePreviews();
  
  // Inicializar contador de metatags
  initializeMetaTagsCounter();
  
  // Marcar como inicializado
  window.articleFormInitialized = true;
}

function initializeQuillEditor() {
  const BlockEmbed = Quill.import('blots/block/embed');

  // === Tweet Blot ===
  class TweetBlot extends BlockEmbed {
    static create(url) {
      const node = super.create();
      node.innerHTML = `<blockquote class="twitter-tweet"><a href="${url}"></a></blockquote>`;
      if (typeof twttr !== 'undefined') {
        twttr.widgets.load(node);
      }
      return node;
    }

    static value(node) {
      const anchor = node.querySelector('a');
      return anchor ? anchor.getAttribute('href') : '';
    }
  }
  TweetBlot.blotName = 'tweet';
  TweetBlot.tagName = 'div';
  TweetBlot.className = 'tweet-embed';
  Quill.register(TweetBlot);

  // === Instagram Blot ===
  class InstagramBlot extends BlockEmbed {
    static create(url) {
      const node = super.create();
      node.innerHTML = `<blockquote class="instagram-media" data-instgrm-permalink="${url}" data-instgrm-version="12"></blockquote>`;
      if (window.instgrm) {
        window.instgrm.Embeds.process(node);
      }
      return node;
    }

    static value(node) {
      return node.querySelector('blockquote')?.getAttribute('data-instgrm-permalink');
    }
  }
  InstagramBlot.blotName = 'instagram';
  InstagramBlot.tagName = 'div';
  InstagramBlot.className = 'instagram-embed';
  Quill.register(InstagramBlot);

  // === TikTok Blot ===
  class TikTokBlot extends BlockEmbed {
    static create(url) {
      const node = super.create();
      const videoId = url.split('/').filter(Boolean).pop();
      node.innerHTML = `
        <blockquote class="tiktok-embed" cite="${url}" data-video-id="${videoId}" style="max-width: 325px;"><section></section></blockquote>
      `;
      const script = document.createElement('script');
      script.src = 'https://www.tiktok.com/embed.js';
      script.async = true;
      node.appendChild(script);
      return node;
    }

    static value(node) {
      return node.querySelector('blockquote')?.getAttribute('cite');
    }
  }
  TikTokBlot.blotName = 'tiktok';
  TikTokBlot.tagName = 'div';
  TikTokBlot.className = 'tiktok-embed';
  Quill.register(TikTokBlot);

  // === Inicialización de Quill ===
  window.editor = new Quill('#note-editor', {
    theme: 'snow',
    modules: {
      toolbar: [
        [{ 'header': [1, 2, 3, 4, 5, 6, false] }],
        ['bold', 'italic', 'underline', 'strike'],
        ['blockquote', 'code-block', 'video', 'image', 'link'],
        ['tweet', 'instagram', 'tiktok'],
        [{ 'list': 'ordered'}, { 'list': 'bullet' }],
        [{ 'indent': '-1'}, { 'indent': '+1' }],
        [{ 'align': [] }],
        [{ 'color': [] }, { 'background': [] }],
        [{ 'font': [] }],
        [{ 'size': ['small', false, 'large', 'huge'] }],
        ['clean']
      ],
      clipboard: {
        matchVisual: false
      }
    },
    placeholder: 'Escribe el contenido de tu artículo aquí...'
  });

  // === Handlers para insertar embeds ===
  const toolbar = window.editor.getModule('toolbar');
  toolbar.addHandler('tweet', function() {
    const url = prompt('Pega el enlace del Tweet:');
    if (url) {
      window.editor.insertEmbed(window.editor.getSelection().index, 'tweet', url);
    }
  });
  toolbar.addHandler('instagram', function() {
    const url = prompt('Pega el enlace del post de Instagram:');
    if (url) {
      window.editor.insertEmbed(window.editor.getSelection().index, 'instagram', url);
    }
  });
  toolbar.addHandler('tiktok', function() {
    const url = prompt('Pega el enlace del video de TikTok:');
    if (url) {
      window.editor.insertEmbed(window.editor.getSelection().index, 'tiktok', url);
    }
  });

  // === Cargar contenido existente ===
  <% if @article.note.present? %>
    try {
      const noteContent = `<%= j @article.note.html_safe %>`;
      if (noteContent.trim() !== '') {
        window.editor.root.innerHTML = noteContent;
      }
    } catch(e) {
      console.error("Error cargando contenido existente:", e);
    }
  <% end %>

  // === Manejo del submit del formulario ===
  const form = document.querySelector('form.directUpload');
  if (form) {
    form.addEventListener('submit', function(e) {
      const contentField = document.querySelector('#article_note');
      if (contentField && window.editor) {
        contentField.value = JSON.stringify({
          html: window.editor.root.innerHTML,
          delta: window.editor.getContents()
        });
      }
      return true;
    });
  }
}

function initializeCounters() {
  // Contador para título
  const titleInput = document.getElementById('article_name');
  const excerptInput = document.getElementById('article_short_description');
  
  if (titleInput) {
    // Crear contador de título si no existe
    if (!document.getElementById('title-counter')) {
      const titleCounter = document.createElement('span');
      titleCounter.id = 'title-counter';
      titleCounter.className = 'char-counter';
      titleCounter.style.cssText = 'display:block;font-size:0.8em;color:#666;';
      titleInput.parentNode.appendChild(titleCounter);
    }
    
    const updateTitleCounter = function() {
      const length = this.value.length;
      const counter = document.getElementById('title-counter');
      if (counter) {
        counter.textContent = length + '/120';
        if (length > 120) {
          this.value = this.value.substring(0, 120);
          counter.textContent = '120/120';
        }
      }
    };
    
    titleInput.addEventListener('input', updateTitleCounter);
    updateTitleCounter.call(titleInput); // Inicializar
  }
  
  if (excerptInput) {
    // Crear contador de extracto si no existe
    if (!document.getElementById('excerpt-counter')) {
      const excerptCounter = document.createElement('span');
      excerptCounter.id = 'excerpt-counter';
      excerptCounter.className = 'char-counter';
      excerptCounter.style.cssText = 'display:block;font-size:0.8em;color:#666;';
      excerptInput.parentNode.appendChild(excerptCounter);
    }
    
    const updateExcerptCounter = function() {
      const length = this.value.length;
      const counter = document.getElementById('excerpt-counter');
      if (counter) {
        counter.textContent = length + '/150';
        if (length > 150) {
          this.value = this.value.substring(0, 150);
          counter.textContent = '150/150';
        }
      }
    };
    
    excerptInput.addEventListener('input', updateExcerptCounter);
    updateExcerptCounter.call(excerptInput); // Inicializar
  }
}

function initializeImagePreviews() {
  // Vista previa de imágenes usando JavaScript puro
  document.querySelectorAll('input[type=file]').forEach(function(input) {
    const field = input.closest('.field');
    if (field && !field.querySelector('.form__image-preview')) {
      const img = document.createElement('img');
      img.className = 'form__image-preview';
      img.style.cssText = 'display:none;max-width:100%;max-height:150px;margin-bottom:10px;border:1px solid #ddd;';
      input.parentNode.insertBefore(img, input);
    }
    
    input.addEventListener('change', function() {
      if (this.files && this.files[0]) {
        const reader = new FileReader();
        reader.onload = function(e) {
          const img = field.querySelector('.form__image-preview');
          if (img) {
            img.src = e.target.result;
            img.style.display = 'block';
          }
        };
        reader.readAsDataURL(this.files[0]);
      }
    });
  });
}

function initializeMetaTagsCounter() {
  const metaTagsInput = document.getElementById('meta_tags');
  const metaTagsCounter = document.getElementById('meta-tags-counter');
  
  if (metaTagsInput && metaTagsCounter) {
    const updateMetaTagsCounter = function() {
      const text = this.value;
      const words = text.trim() === '' ? 0 : text.trim().split(/\s+/).length;
      
      if (words > 3) {
        const wordsArray = text.trim().split(/\s+/).slice(0, 3);
        this.value = wordsArray.join(' ');
        metaTagsCounter.textContent = '3/3 palabras (límite alcanzado)';
        metaTagsCounter.style.color = '#e74c3c';
      } else {
        metaTagsCounter.textContent = words + '/3 palabras';
        metaTagsCounter.style.color = words === 3 ? '#e74c3c' : '#666';
      }
    };
    
    metaTagsInput.addEventListener('input', updateMetaTagsCounter);
    updateMetaTagsCounter.call(metaTagsInput); // Inicializar
  }
}

// Múltiples estrategias de inicialización
document.addEventListener('DOMContentLoaded', initializeArticleForm);
document.addEventListener('turbolinks:load', initializeArticleForm);

// Inicialización inmediata si la página ya está cargada
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', initializeArticleForm);
} else {
  setTimeout(initializeArticleForm, 0);
}

// Limpiar al salir de la página
document.addEventListener('turbolinks:before-cache', function() {
  window.articleFormInitialized = false;
  if (window.editor) {
    // No destruimos el editor completamente, solo marcamos como no inicializado
    window.articleFormInitialized = false;
  }
});
</script>

<%= render "components/s3_form_js" %>
QUIERO GUARDAR EMBEBIDOS DE X, TIK TOK E INSTAGRAM PERO CUANDO LOS SUBO EN EL ARTICULO NO SE SUBENNN, AQUI SE MUESTRAN <div class="gallery">
	<div class="gallery__image">
		<img src="">
	</div>
	<div class="gallery__controls">
		<div class="previous"></div>

		<div class="next"></div>
	</div>
</div>
<script>
	$(document).on("click", function(event) {
		if( $(event.target).is(".gallery__thumbnail") ) {
			TweenMax.set(".gallery", { display:  "flex" }); 
			TweenMax.to(".gallery", .6, { opacity: 1, pointerEvents: "all" });
			$(".gallery__image img").attr("src", $(event.target).attr("src"));
			current_image = $(event.target).data("image");
			console.log(current_image); 
		}	
		if($(event.target).is(".gallery__image") && !($(event.target).is(".gallery__image img") || $(event.target).is(".gallery__controls") || $(event.target).is(".gallery__controls *"))) {
			TweenMax.to(".gallery", .6, { opacity: 0, pointerEvents: "none", onComplete: function(){
				TweenMax.set(".gallery", { display:  "none" }); 
			} });
		}
		if( $(event.target).is(".gallery__controls .next") ) {		
			console.log($(".gallery__thumbnail").length); 
			console.log(current_image); 
			if ($(".gallery__thumbnail").length > current_image) {
				$(".gallery__image img").attr("src", $(".gallery__thumbnail").eq(current_image + 1).attr("src")); 				
				current_image++;
			} else {
				$(".gallery__image img").attr("src", $(".gallery__thumbnail").eq(0).attr("src"));
				current_image = 0; 
			}
		}
		if( $(event.target).is(".gallery__controls .previous") ) {		
			console.log($(".gallery__thumbnail").length); 
			console.log(current_image); 
			if (0 < current_image) {
				$(".gallery__image img").attr("src", $(".gallery__thumbnail").eq(current_image - 1).attr("src")); 				
				current_image--;
			} else {
				$(".gallery__image img").attr("src", $(".gallery__thumbnail").eq($(".gallery__thumbnail").length).attr("src"));
				current_image = $(".gallery__thumbnail").length; 
			}
		}
	}); 
</script>

		<div class="interface--row">
    <div class="note note--display">
        <% if @article.exclusive? %>
            <p class="exclusive--note">Exclusiva</p>
        <% end %>
        <h1 class="title"><%= @article.name %></h1>    
        <p class="short-description"><%= @article.short_description %></p>

        <% if @article.articable_id != 1019   %>

            <div class="article__info">
                <% if @article.author and @article.author.name != "Ninguno" %>
                    <p class="author">Por <%= @article.author.name %></p>

                <% else %>
                    <% if @article.user %>
                        <p class="author">Por <%= @article.user.full_name %></p>
                    <% else %>
                        <p class="author">--</p>
                    <% end %>
                <% end %>
                <% if @article.published_at %>
                    <p class="date"><%= l(@article.published_at, format: "%H:%M %A %e %B, %Y") %></p>
                <% else %>
                    <p class="date"><%= l(@article.created_at, format: "%H:%M %A %e %B, %Y") %></p>
                <% end %>
                <% if @article.articable_type == "Section" and @article.articable %>
                    <%= link_to @article.articable.name, section_path(Section.find(@article.articable_id)), class: "section" %>
                <% end %>
                <div class="social-media" data-article="<%= article_url(@article) %>">
                    <%= link_to ("whatsapp://send?text= Estoy leyendo en GlobalMedia: " + article_url(@article)), data: { action: "share/whatsapp/share" }, class: "social-media__link whatsapp", target: "_blank" do %>
                        <%= image_tag "whatsapp.svg" %>
                    <% end %>
                    <a class="social-media__link facebook" target="_blank" onclick="shareToFacebook(event);">
                        <%= image_tag "facebook.svg" %>                
                    </a>    

                    <a href="https://twitter.com/share?url=<%= CGI.unescape(article_url(@article)) %>&via=GlobalMediaMx&text=<%= @article.name %>" class="social-media__link twitter" target="_blank">
                        <%= image_tag "twitter.svg" %>
                    </a>
                    <a class="social-media__link copy" target="_self" data-clipboard-text="<%= CGI.unescape(article_url(@article, host: 'https://globalmedia.mx')) %>"

                        <input class="hidden" value="<%= article_url(@article, host: 'https://globalmedia.mx') %>">
                        <%= image_tag "copy.svg" %>
                    </a> 
                </div>
                
            </div>
            <% if @article.keyword %>
                <div class="keyword-container">#EsNoticia: <%= @article.keyword.keyword %></div>
            <% end %>
                <div class="hashtags-container">
                    <p>Hablamos de:</p> <%= render @article.hashtags %>
                </div>
            

        <% end %>


        <% if @article.video_url != "" %> 
            <div class="iframe">
                <iframe width="650" height="415" src="<%= @article.    video_url %>" frameborder="0" allowfullscreen></iframe>
            </div>
        <% else %>        
            <% if @article.image %>
                <%= image_tag @article.image, class: "main-image", alt: @article.name %>
            <% end %>
        <% end %>
                <div class="gallery__thumbnails">
                    <% @article.images.each_with_index do |image, index| %>
                        <%= image_tag image.src, class: "gallery__thumbnail", data: { image: index }  %>
                    <% end %>    
        <% if @article.note.present? %>
        <div class="quill-content">
            <%= display_quill_content(@article.note) %>
        </div>
        <% elsif @article.note_old.present? %>
        <div class="quill-content">
            <%= simple_format(@article.note_old) %>
        </div>
        <% end %>


        <!-- NOTAS DEL PROGRAMADOR-->
            <!-- NO QUITAR ESTOS IFS PORQUE SON LOS QUE VEN SI HAY BANNER O NO -->

            <!-- CAMBIAR EL NAME POR EL PARAMETRO QUE QUIERAS MOSTRAR->
        <!-- -->
        <div class="fb-comments" data-href="<%= request.original_url.force_encoding 'UTF-8' %>" data-numposts="5"></div>
        <% if get_banner(@article.articable, @article.articable_type, "Grande" ).count > 0 %>

            <%= render "components/big-banner", article: @article, banner: get_banner(@article.articable, @article.articable_type, "Grande" )  %>
        <% else %>
            <% if get_banner(@article.articable, "Global", "Grande" ).count > 0 %>
                <%= render "components/big-banner", article: @article, banner: get_banner(@article.articable, "Global", "Grande" ) %>
            <% else %>
                <%= render "components/big-banner", article: @article, banner: "" %>

            <% end %>
        <% end %>
        <% if @related_sections.count > 0 %>
            <% @related_sections.each do |related_section| %>
                <%= render "components/section-recommendations", articles: get_articles_per_section(related_section.section_reference_id, 3), section_name: related_section.section_reference.name, highlight: "" %>

            <% end %>
        <% end %>

    </div>
    
</div>
				   <!-- Sidebar movido fuera del div note--display -->
    <% if get_banner(@article.articable, @article.articable_type, "Mediano" ).count > 0 %>
        <%= render "components/sidebar", article: @article, banner: get_banner(@article.articable, @article.articable_type, "Mediano" ) %>
    <% else %>
        <% if get_banner(@article.articable, "Global", "Mediano" ).count > 0 %>
            <%= render "components/sidebar", article: @article, banner: get_banner(@article.articable, "Global", "Mediano" ) %>
        <% else %>
            <%= render "components/sidebar", article: @article, banner: ""  %>
        <% end %>
    <% end %>

		<template id="related-articles">

			<% if false and @article.created_at > ("2017-09-10").to_date %>
					<% if related_by_hashtags(@article).count > 0 %>
						<p class="related-articles"><strong class="related-articles__title">Artículos relacionados:</strong><br><% related_by_hashtags(@article).each do |name, slug, id| %><% if @article.id != id %><br><%= link_to truncate(name, length:  80), article_path(slug), class: "related-article" %><% end %><% end %></p>
					<% end %>

			<% end %>
		</template>
						
<style>
.interface--row {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    justify-content: space-between;
    width: 100%;
    gap: 30px;
}

.note--display {
    flex: 1;             
    min-width: 0;         
    max-width: calc(100% - 342px); 
}

.sidebar {
    width: 342px;
    flex-shrink: 0;       
    position: sticky;
    top: 20px; 
    align-self: flex-start; 
}


@media (max-width: 768px) {
    .interface--row {
        flex-direction: column;
        gap: 0; 
    }
    
    .note--display {
        max-width: 100%; 
        width: 100%;
        order: 1; 
    }
    
    .sidebar {
        width: 100%;
        position: static; 
        order: 2; 
        margin-top: 30px;
    }
	
}
</style>

<script async defer src="https://platform.instagram.com/en_US/embeds.js"></script>

<script>
	<% if @article.note %>
		var content = <%= raw(@article.note) %>;
		var container = document.getElementById("quill-container")
		display_quill_note(content, container);

		<% if false and @article.created_at > ("2017-09-10").to_date  %>
			var numberOfParagraphs = $("#quill-container p").length; 

			var middle = parseInt(Math.ceil(numberOfParagraphs/2)); 
			var relatedArticles = document.querySelector("#related-articles");
			var clone = document.importNode(relatedArticles.content, true); 

			$("#quill-container p:nth-of-type("+middle+")").after(clone);
			var clone = document.importNode(relatedArticles.content, true); 
			$("#quill-container").after(clone);
			// $("#quill-container > p:nth-of-type("+middle+")");
		<% end %>
	<% end %>

</script>

<%= render "components/s3_form_js" %>
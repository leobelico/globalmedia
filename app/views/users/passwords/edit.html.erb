
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "session-form" }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token %>
  <h1 class="title">Nueva contraseña</h1>
  <div class="field field--string">
    <%= f.label :password, "Nueva contraseña" %>
    <%= f.password_field :password, autofocus: true, autocomplete: "off" %>
  </div>

  <div class="field  field--string">
    <%= f.label :password_confirmation, "Confirma tu contraseña" %>
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar contraseña" %>
  </div>
<%= render "users/shared/links" %>
<% end %>

